# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing_extensions import Annotated
from pve_client.models.pve_boolean import PveBoolean
from pve_client.models.pve_model_enum import PveModelEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PveQemuNetConfig(BaseModel):
    """
    PveQemuNetConfig
    """ # noqa: E501

    bridge: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Bridge to attach the network device to. The Proxmox VE standard bridge is called 'vmbr0'.  If you do not specify a bridge, we create a kvm user (NATed) network device, which provides DHCP and DNS services. The following addresses are used:   10.0.2.2   Gateway  10.0.2.3   DNS Server  10.0.2.4   SMB Server  The DHCP server assign addresses to the guest starting from 10.0.2.15. ")

    e1000: Optional[StrictStr] = None

    e1000_82540em: Optional[StrictStr] = Field(default=None, alias="e1000-82540em")

    e1000_82544gc: Optional[StrictStr] = Field(default=None, alias="e1000-82544gc")

    e1000_82545em: Optional[StrictStr] = Field(default=None, alias="e1000-82545em")

    e1000e: Optional[StrictStr] = None

    firewall: Optional[PveBoolean] = Field(default=None, description="Whether this interface should be protected by the firewall.")

    i82551: Optional[StrictStr] = None

    i82557b: Optional[StrictStr] = None

    i82559er: Optional[StrictStr] = None

    link_down: Optional[PveBoolean] = Field(default=None, description="Whether this interface should be disconnected (like pulling the plug).")

    macaddr: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="A common MAC address with the I/G (Individual/Group) bit not set.")

    model: PveModelEnum = Field(description="Network Card Model. The 'virtio' model provides the best performance with very low CPU overhead. If your guest does not support this driver, it is usually best to use 'e1000'.")

    mtu: Optional[Annotated[int, Field(le=65520, strict=True, ge=1)]] = Field(default=None, description="Force MTU of network device (VirtIO only). Setting to '1' or empty will use the bridge MTU")

    ne2k_isa: Optional[StrictStr] = None

    ne2k_pci: Optional[StrictStr] = None

    pcnet: Optional[StrictStr] = None

    queues: Optional[Annotated[int, Field(le=64, strict=True, ge=0)]] = Field(default=None, description="Number of packet queues to be used on the device.")

    rate: Optional[Union[Annotated[float, Field(strict=True, ge=0)], Annotated[int, Field(strict=True, ge=0)]]] = Field(default=None, description="Rate limit in mbps (megabytes per second) as floating point number.")

    rtl8139: Optional[StrictStr] = None

    tag: Optional[Annotated[int, Field(le=4094, strict=True, ge=1)]] = Field(default=None, description="VLAN tag to apply to packets on this interface.")

    trunks: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="VLAN trunks to pass through this interface.")

    virtio: Optional[StrictStr] = None

    vmxnet3: Optional[StrictStr] = None

    __properties: ClassVar[List[str]] = ["bridge", "e1000", "e1000-82540em", "e1000-82544gc", "e1000-82545em", "e1000e", "firewall", "i82551", "i82557b", "i82559er", "link_down", "macaddr", "model", "mtu", "ne2k_isa", "ne2k_pci", "pcnet", "queues", "rate", "rtl8139", "tag", "trunks", "virtio", "vmxnet3"]


    @field_validator('bridge')
    def bridge_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[A-Za-z][A-Za-z0-9_]{0,20}$", value):
            raise ValueError(r"must validate the regular expression /^[A-Za-z][A-Za-z0-9_]{0,20}$/")
        return value












    @field_validator('macaddr')
    def macaddr_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[0-9a-fA-F]{2}(?:[:\-][0-9a-fA-F]{2}){5}$", value):
            raise ValueError(r"must validate the regular expression /^[0-9a-fA-F]{2}(?:[:\-][0-9a-fA-F]{2}){5}$/")
        return value











    @field_validator('trunks')
    def trunks_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"(?:\d+(?:-\d+)?(?:;\d+(?:-\d+)?)*)", value):
            raise ValueError(r"must validate the regular expression /(?:\d+(?:-\d+)?(?:;\d+(?:-\d+)?)*)/")
        return value





    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PveQemuNetConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PveQemuNetConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "bridge": obj.get("bridge"),
            "e1000": obj.get("e1000"),
            "e1000-82540em": obj.get("e1000-82540em"),
            "e1000-82544gc": obj.get("e1000-82544gc"),
            "e1000-82545em": obj.get("e1000-82545em"),
            "e1000e": obj.get("e1000e"),
            "firewall": obj.get("firewall"),
            "i82551": obj.get("i82551"),
            "i82557b": obj.get("i82557b"),
            "i82559er": obj.get("i82559er"),
            "link_down": obj.get("link_down"),
            "macaddr": obj.get("macaddr"),
            "model": obj.get("model"),
            "mtu": obj.get("mtu"),
            "ne2k_isa": obj.get("ne2k_isa"),
            "ne2k_pci": obj.get("ne2k_pci"),
            "pcnet": obj.get("pcnet"),
            "queues": obj.get("queues"),
            "rate": obj.get("rate"),
            "rtl8139": obj.get("rtl8139"),
            "tag": obj.get("tag"),
            "trunks": obj.get("trunks"),
            "virtio": obj.get("virtio"),
            "vmxnet3": obj.get("vmxnet3")
        })
        return _obj


    def to_shorthand(self) -> str:
        """Serialise this PveQemuNetConfig into Proxmox's CLI-style shorthand
        string (``key=value,…``).

        The property marked ``x-pve-default-key`` is emitted positionally
        without a ``key=`` prefix; aliases collapse multiple property
        names to the same wire key.

        Example::

            >>> PveQemuNetConfig(model='virtio', bridge='vmbr0', firewall=1).to_shorthand()
            'virtio,bridge=vmbr0,firewall=1'
        """
        parts: list[str] = []
        
        if self.model is not None:
            parts.append(str(self.model))
        
        
        if self.bridge is not None:
            parts.append(f"bridge={self.bridge}")
        
        if self.e1000 is not None:
            parts.append(f"macaddr={self.e1000}")
        
        if self.e1000_82540em is not None:
            parts.append(f"macaddr={self.e1000_82540em}")
        
        if self.e1000_82544gc is not None:
            parts.append(f"macaddr={self.e1000_82544gc}")
        
        if self.e1000_82545em is not None:
            parts.append(f"macaddr={self.e1000_82545em}")
        
        if self.e1000e is not None:
            parts.append(f"macaddr={self.e1000e}")
        
        if self.firewall is not None:
            parts.append(f"firewall={self.firewall}")
        
        if self.i82551 is not None:
            parts.append(f"macaddr={self.i82551}")
        
        if self.i82557b is not None:
            parts.append(f"macaddr={self.i82557b}")
        
        if self.i82559er is not None:
            parts.append(f"macaddr={self.i82559er}")
        
        if self.link_down is not None:
            parts.append(f"link_down={self.link_down}")
        
        if self.macaddr is not None:
            parts.append(f"macaddr={self.macaddr}")
        
        if self.mtu is not None:
            parts.append(f"mtu={self.mtu}")
        
        if self.ne2k_isa is not None:
            parts.append(f"macaddr={self.ne2k_isa}")
        
        if self.ne2k_pci is not None:
            parts.append(f"macaddr={self.ne2k_pci}")
        
        if self.pcnet is not None:
            parts.append(f"macaddr={self.pcnet}")
        
        if self.queues is not None:
            parts.append(f"queues={self.queues}")
        
        if self.rate is not None:
            parts.append(f"rate={self.rate}")
        
        if self.rtl8139 is not None:
            parts.append(f"macaddr={self.rtl8139}")
        
        if self.tag is not None:
            parts.append(f"tag={self.tag}")
        
        if self.trunks is not None:
            parts.append(f"trunks={self.trunks}")
        
        if self.virtio is not None:
            parts.append(f"macaddr={self.virtio}")
        
        if self.vmxnet3 is not None:
            parts.append(f"macaddr={self.vmxnet3}")
        
        return ",".join(parts)

    @classmethod
    def from_shorthand(cls, raw: Optional[str]) -> Optional["PveQemuNetConfig"]:
        """Parse a Proxmox shorthand string into a PveQemuNetConfig. Mirror of
        :meth:`to_shorthand`. The first comma-separated segment without
        ``=`` is interpreted as the default-key value.
        """
        if raw is None:
            return None
        kwargs: dict = {}
        default_key_consumed = False
        for part in raw.split(","):
            if not part:
                continue
            if "=" not in part:
                
                if not default_key_consumed:
                    kwargs["model"] = part
                    default_key_consumed = True
                    continue
                
                continue
            k, _, v = part.partition("=")
            kwargs[k.replace("-", "_")] = v
        return cls(**kwargs)

