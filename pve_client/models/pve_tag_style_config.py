# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 8.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from pve_client.models.pve_boolean import PveBoolean
from pve_client.models.pve_cluster_options_ordering_enum import PveClusterOptionsOrderingEnum
from pve_client.models.pve_cluster_options_shape_enum import PveClusterOptionsShapeEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PveTagStyleConfig(BaseModel):
    """
    PveTagStyleConfig
    """ # noqa: E501

    case_sensitive: Optional[PveBoolean] = Field(default=None, description="Controls if filtering for unique tags on update should check case-sensitive.", alias="case-sensitive")

    color_map: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Manual color mapping for tags (semicolon separated).", alias="color-map")

    ordering: Optional[PveClusterOptionsOrderingEnum] = Field(default=None, description="Controls the sorting of the tags in the web-interface and the API update.")

    shape: Optional[PveClusterOptionsShapeEnum] = Field(default=None, description="Tag shape for the web ui tree. 'full' draws the full tag. 'circle' draws only a circle with the background color. 'dense' only draws a small rectancle (useful when many tags are assigned to each guest).'none' disables showing the tags.")

    __properties: ClassVar[List[str]] = ["case-sensitive", "color-map", "ordering", "shape"]



    @field_validator('color_map')
    def color_map_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"(?:(?:[a-z0-9_][a-z0-9_\-\+\.]*):[0-9a-fA-F]{6}(?::[0-9a-fA-F]{6})?)(?:;(?:(?:[a-z0-9_][a-z0-9_\-\+\.]*):[0-9a-fA-F]{6}(?::[0-9a-fA-F]{6})?))*", value):
            raise ValueError(r"must validate the regular expression /(?:(?:[a-z0-9_][a-z0-9_\-\+\.]*):[0-9a-fA-F]{6}(?::[0-9a-fA-F]{6})?)(?:;(?:(?:[a-z0-9_][a-z0-9_\-\+\.]*):[0-9a-fA-F]{6}(?::[0-9a-fA-F]{6})?))*/")
        return value





    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PveTagStyleConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PveTagStyleConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "case-sensitive": obj.get("case-sensitive"),
            "color-map": obj.get("color-map"),
            "ordering": obj.get("ordering"),
            "shape": obj.get("shape")
        })
        return _obj


    def to_shorthand(self) -> str:
        """Serialise this PveTagStyleConfig into Proxmox's CLI-style shorthand
        string (``key=value,…``).

        The property marked ``x-pve-default-key`` is emitted positionally
        without a ``key=`` prefix; aliases collapse multiple property
        names to the same wire key.

        Example::

            >>> PveTagStyleConfig(model='virtio', bridge='vmbr0', firewall=1).to_shorthand()
            'virtio,bridge=vmbr0,firewall=1'
        """
        parts: list[str] = []
        
        
        if self.case_sensitive is not None:
            parts.append(f"case-sensitive={self.case_sensitive}")
        
        if self.color_map is not None:
            parts.append(f"color-map={self.color_map}")
        
        if self.ordering is not None:
            parts.append(f"ordering={self.ordering}")
        
        if self.shape is not None:
            parts.append(f"shape={self.shape}")
        
        return ",".join(parts)

    @classmethod
    def from_shorthand(cls, raw: Optional[str]) -> Optional["PveTagStyleConfig"]:
        """Parse a Proxmox shorthand string into a PveTagStyleConfig. Mirror of
        :meth:`to_shorthand`. The first comma-separated segment without
        ``=`` is interpreted as the default-key value.
        """
        if raw is None:
            return None
        kwargs: dict = {}
        default_key_consumed = False
        for part in raw.split(","):
            if not part:
                continue
            if "=" not in part:
                
                continue
            k, _, v = part.partition("=")
            kwargs[k.replace("-", "_")] = v
        return cls(**kwargs)

