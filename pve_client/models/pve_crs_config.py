# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing_extensions import Annotated
from pve_client.models.pve_boolean import PveBoolean
from pve_client.models.pve_cluster_options_ha_auto_rebalance_method_enum import PveClusterOptionsHaAutoRebalanceMethodEnum
from pve_client.models.pve_cluster_options_ha_enum import PveClusterOptionsHaEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PveCrsConfig(BaseModel):
    """
    PveCrsConfig
    """ # noqa: E501

    ha: Optional[PveClusterOptionsHaEnum] = Field(default=None, description="Configures how the HA Manager should select nodes to start or recover services:  - with 'basic', only the number of services is used, - with 'static', static CPU and memory configuration of services are considered, - with 'dynamic', static and dynamic CPU and memory usage of services are considered. ")

    ha_auto_rebalance: Optional[PveBoolean] = Field(default=None, description="Whether to use CRS for balancing HA resources automatically depending on the current node imbalance.", alias="ha-auto-rebalance")

    ha_auto_rebalance_hold_duration: Optional[Union[Annotated[float, Field(strict=True, ge=0)], Annotated[int, Field(strict=True, ge=0)]]] = Field(default=3, description="The number of HA rounds for which the cluster node imbalance threshold must be exceeded before triggering an automatic resource balancing migration.", alias="ha-auto-rebalance-hold-duration")

    ha_auto_rebalance_margin: Optional[Union[Annotated[float, Field(le=100, strict=True, ge=0)], Annotated[int, Field(le=100, strict=True, ge=0)]]] = Field(default=10, description="The minimum relative improvement in cluster node imbalance, in percent, to commit to a resource balancing migration.", alias="ha-auto-rebalance-margin")

    ha_auto_rebalance_method: Optional[PveClusterOptionsHaAutoRebalanceMethodEnum] = Field(default=None, description="The method to use for the scoring of balancing migrations.", alias="ha-auto-rebalance-method")

    ha_auto_rebalance_threshold: Optional[Union[Annotated[float, Field(le=100, strict=True, ge=0)], Annotated[int, Field(le=100, strict=True, ge=0)]]] = Field(default=30, description="The cluster node imbalance, in percent, which will trigger the automatic resource balancing system if exceeded.", alias="ha-auto-rebalance-threshold")

    ha_rebalance_on_start: Optional[PveBoolean] = Field(default=None, description="Set to use CRS for selecting a suited node when a HA services request-state changes from stop to start.", alias="ha-rebalance-on-start")

    __properties: ClassVar[List[str]] = ["ha", "ha-auto-rebalance", "ha-auto-rebalance-hold-duration", "ha-auto-rebalance-margin", "ha-auto-rebalance-method", "ha-auto-rebalance-threshold", "ha-rebalance-on-start"]










    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PveCrsConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PveCrsConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "ha": obj.get("ha"),
            "ha-auto-rebalance": obj.get("ha-auto-rebalance"),
            "ha-auto-rebalance-hold-duration": obj.get("ha-auto-rebalance-hold-duration") if obj.get("ha-auto-rebalance-hold-duration") is not None else 3,
            "ha-auto-rebalance-margin": obj.get("ha-auto-rebalance-margin") if obj.get("ha-auto-rebalance-margin") is not None else 10,
            "ha-auto-rebalance-method": obj.get("ha-auto-rebalance-method"),
            "ha-auto-rebalance-threshold": obj.get("ha-auto-rebalance-threshold") if obj.get("ha-auto-rebalance-threshold") is not None else 30,
            "ha-rebalance-on-start": obj.get("ha-rebalance-on-start")
        })
        return _obj


    def to_shorthand(self) -> str:
        """Serialise this PveCrsConfig into Proxmox's CLI-style shorthand
        string (``key=value,…``).

        The property marked ``x-pve-default-key`` is emitted positionally
        without a ``key=`` prefix; aliases collapse multiple property
        names to the same wire key.

        Example::

            >>> PveCrsConfig(model='virtio', bridge='vmbr0', firewall=1).to_shorthand()
            'virtio,bridge=vmbr0,firewall=1'
        """
        parts: list[str] = []
        
        
        if self.ha is not None:
            parts.append(f"ha={self.ha}")
        
        if self.ha_auto_rebalance is not None:
            parts.append(f"ha-auto-rebalance={self.ha_auto_rebalance}")
        
        if self.ha_auto_rebalance_hold_duration is not None:
            parts.append(f"ha-auto-rebalance-hold-duration={self.ha_auto_rebalance_hold_duration}")
        
        if self.ha_auto_rebalance_margin is not None:
            parts.append(f"ha-auto-rebalance-margin={self.ha_auto_rebalance_margin}")
        
        if self.ha_auto_rebalance_method is not None:
            parts.append(f"ha-auto-rebalance-method={self.ha_auto_rebalance_method}")
        
        if self.ha_auto_rebalance_threshold is not None:
            parts.append(f"ha-auto-rebalance-threshold={self.ha_auto_rebalance_threshold}")
        
        if self.ha_rebalance_on_start is not None:
            parts.append(f"ha-rebalance-on-start={self.ha_rebalance_on_start}")
        
        return ",".join(parts)

    @classmethod
    def from_shorthand(cls, raw: Optional[str]) -> Optional["PveCrsConfig"]:
        """Parse a Proxmox shorthand string into a PveCrsConfig. Mirror of
        :meth:`to_shorthand`. The first comma-separated segment without
        ``=`` is interpreted as the default-key value.
        """
        if raw is None:
            return None
        kwargs: dict = {}
        default_key_consumed = False
        for part in raw.split(","):
            if not part:
                continue
            if "=" not in part:
                
                continue
            k, _, v = part.partition("=")
            kwargs[k.replace("-", "_")] = v
        return cls(**kwargs)

