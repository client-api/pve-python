# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from pve_client.models.pve_boolean import PveBoolean
from pve_client.models.pve_efitype_enum import PveEfitypeEnum
from pve_client.models.pve_format_enum import PveFormatEnum
from pve_client.models.pve_ms_cert_enum import PveMsCertEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PveEfidiskConfig(BaseModel):
    """
    PveEfidiskConfig
    """ # noqa: E501

    efitype: Optional[PveEfitypeEnum] = Field(default=None, description="Size and type of the OVMF EFI vars. '4m' is newer and recommended, and required for Secure Boot. For backwards compatibility, '2m' is used if not otherwise specified. Ignored for VMs with arch=aarch64 (ARM).")

    file: Annotated[str, Field(strict=True)] = Field(description="The drive's backing volume.")

    format: Optional[PveFormatEnum] = Field(default=None, description="The drive's backing file's data format.")

    import_from: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Create a new disk, importing from this source (volume ID or absolute path). When an absolute path is specified, it's up to you to ensure that the source is not actively used by another process during the import!", alias="import-from")

    ms_cert: Optional[PveMsCertEnum] = Field(default=None, description="Informational marker indicating the version of the latest Microsoft UEFI certificates that have been enrolled by Proxmox VE. The value '2023k' means that the 'Microsoft UEFI CA 2023', the 'Windows UEFI CA 2023' and the 'Microsoft Corporation KEK 2K CA 2023' certificates are included. The values '2023' and '2023w' are deprecated and for compatibility only.", alias="ms-cert")

    pre_enrolled_keys: Optional[PveBoolean] = Field(default=None, description="Use am EFI vars template with distribution-specific and Microsoft Standard keys enrolled, if used with 'efitype=4m'. Note that this will enable Secure Boot by default, though it can still be turned off from within the VM.", alias="pre-enrolled-keys")

    size: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Disk size. This is purely informational and has no effect.")

    volume: Optional[StrictStr] = None

    __properties: ClassVar[List[str]] = ["efitype", "file", "format", "import-from", "ms-cert", "pre-enrolled-keys", "size", "volume"]



    @field_validator('file')
    def file_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(?:[A-Za-z0-9_\-]+:.+|\/.+)$", value):
            raise ValueError(r"must validate the regular expression /^(?:[A-Za-z0-9_\-]+:.+|\/.+)$/")
        return value



    @field_validator('import_from')
    def import_from_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(?:[A-Za-z0-9_\-]+:.+|\/.+)$", value):
            raise ValueError(r"must validate the regular expression /^(?:[A-Za-z0-9_\-]+:.+|\/.+)$/")
        return value




    @field_validator('size')
    def size_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^\d+(?:\.\d+)?[KMGT]?$", value):
            raise ValueError(r"must validate the regular expression /^\d+(?:\.\d+)?[KMGT]?$/")
        return value




    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PveEfidiskConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PveEfidiskConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "efitype": obj.get("efitype"),
            "file": obj.get("file"),
            "format": obj.get("format"),
            "import-from": obj.get("import-from"),
            "ms-cert": obj.get("ms-cert"),
            "pre-enrolled-keys": obj.get("pre-enrolled-keys"),
            "size": obj.get("size"),
            "volume": obj.get("volume")
        })
        return _obj


    def to_shorthand(self) -> str:
        """Serialise this PveEfidiskConfig into Proxmox's CLI-style shorthand
        string (``key=value,…``).

        The property marked ``x-pve-default-key`` is emitted positionally
        without a ``key=`` prefix; aliases collapse multiple property
        names to the same wire key.

        Example::

            >>> PveEfidiskConfig(model='virtio', bridge='vmbr0', firewall=1).to_shorthand()
            'virtio,bridge=vmbr0,firewall=1'
        """
        parts: list[str] = []
        
        if self.file is not None:
            parts.append(str(self.file))
        
        
        if self.efitype is not None:
            parts.append(f"efitype={self.efitype}")
        
        if self.format is not None:
            parts.append(f"format={self.format}")
        
        if self.import_from is not None:
            parts.append(f"import-from={self.import_from}")
        
        if self.ms_cert is not None:
            parts.append(f"ms-cert={self.ms_cert}")
        
        if self.pre_enrolled_keys is not None:
            parts.append(f"pre-enrolled-keys={self.pre_enrolled_keys}")
        
        if self.size is not None:
            parts.append(f"size={self.size}")
        
        if self.volume is not None:
            parts.append(f"file={self.volume}")
        
        return ",".join(parts)

    @classmethod
    def from_shorthand(cls, raw: Optional[str]) -> Optional["PveEfidiskConfig"]:
        """Parse a Proxmox shorthand string into a PveEfidiskConfig. Mirror of
        :meth:`to_shorthand`. The first comma-separated segment without
        ``=`` is interpreted as the default-key value.
        """
        if raw is None:
            return None
        kwargs: dict = {}
        default_key_consumed = False
        for part in raw.split(","):
            if not part:
                continue
            if "=" not in part:
                
                if not default_key_consumed:
                    kwargs["file"] = part
                    default_key_consumed = True
                    continue
                
                continue
            k, _, v = part.partition("=")
            kwargs[k.replace("-", "_")] = v
        return cls(**kwargs)

