# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 8.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing_extensions import Annotated
from pve_client.models.pve_boolean import PveBoolean
from pve_client.models.pve_lxc_type_enum import PveLxcTypeEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PveNetConfig(BaseModel):
    """
    PveNetConfig
    """ # noqa: E501

    bridge: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Bridge to attach the network device to.")

    firewall: Optional[PveBoolean] = Field(default=None, description="Controls whether this interface's firewall rules should be used.")

    gw: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Default gateway for IPv4 traffic.")

    gw6: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Default gateway for IPv6 traffic.")

    host_managed: Optional[PveBoolean] = Field(default=None, description="Whether this interface's IP configuration should be managed by the host.", alias="host-managed")

    hwaddr: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="A common MAC address with the I/G (Individual/Group) bit not set.")

    ip: Optional[StrictStr] = Field(default=None, description="IPv4 address in CIDR format.")

    ip6: Optional[StrictStr] = Field(default=None, description="IPv6 address in CIDR format.")

    link_down: Optional[PveBoolean] = Field(default=None, description="Whether this interface should be disconnected (like pulling the plug).")

    mtu: Optional[Annotated[int, Field(le=65535, strict=True, ge=64)]] = Field(default=None, description="Maximum transfer unit of the interface. (lxc.network.mtu)")

    name: Annotated[str, Field(strict=True)] = Field(description="Name of the network device as seen from inside the container. (lxc.network.name)")

    rate: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Apply rate limiting to the interface")

    tag: Optional[Annotated[int, Field(le=4094, strict=True, ge=1)]] = Field(default=None, description="VLAN tag for this interface.")

    trunks: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="VLAN ids to pass through the interface")

    type: Optional[PveLxcTypeEnum] = Field(default=None, description="Network interface type.")

    __properties: ClassVar[List[str]] = ["bridge", "firewall", "gw", "gw6", "host-managed", "hwaddr", "ip", "ip6", "link_down", "mtu", "name", "rate", "tag", "trunks", "type"]


    @field_validator('bridge')
    def bridge_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"[-_.\w\d]+", value):
            raise ValueError(r"must validate the regular expression /[-_.\w\d]+/")
        return value



    @field_validator('gw')
    def gw_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$", value):
            raise ValueError(r"must validate the regular expression /^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$/")
        return value


    @field_validator('gw6')
    def gw6_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(?:[0-9a-fA-F]{0,4}:){2,7}[0-9a-fA-F]{0,4}$", value):
            raise ValueError(r"must validate the regular expression /^(?:[0-9a-fA-F]{0,4}:){2,7}[0-9a-fA-F]{0,4}$/")
        return value



    @field_validator('hwaddr')
    def hwaddr_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[0-9a-fA-F]{2}(?:[:\-][0-9a-fA-F]{2}){5}$", value):
            raise ValueError(r"must validate the regular expression /^[0-9a-fA-F]{2}(?:[:\-][0-9a-fA-F]{2}){5}$/")
        return value






    @field_validator('name')
    def name_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"[-_.\w\d]+", value):
            raise ValueError(r"must validate the regular expression /[-_.\w\d]+/")
        return value




    @field_validator('trunks')
    def trunks_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"(?:\d+(?:;\d+)*)", value):
            raise ValueError(r"must validate the regular expression /(?:\d+(?:;\d+)*)/")
        return value




    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PveNetConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PveNetConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "bridge": obj.get("bridge"),
            "firewall": obj.get("firewall"),
            "gw": obj.get("gw"),
            "gw6": obj.get("gw6"),
            "host-managed": obj.get("host-managed"),
            "hwaddr": obj.get("hwaddr"),
            "ip": obj.get("ip"),
            "ip6": obj.get("ip6"),
            "link_down": obj.get("link_down"),
            "mtu": obj.get("mtu"),
            "name": obj.get("name"),
            "rate": obj.get("rate"),
            "tag": obj.get("tag"),
            "trunks": obj.get("trunks"),
            "type": obj.get("type")
        })
        return _obj


    def to_shorthand(self) -> str:
        """Serialise this PveNetConfig into Proxmox's CLI-style shorthand
        string (``key=value,…``).

        The property marked ``x-pve-default-key`` is emitted positionally
        without a ``key=`` prefix; aliases collapse multiple property
        names to the same wire key.

        Example::

            >>> PveNetConfig(model='virtio', bridge='vmbr0', firewall=1).to_shorthand()
            'virtio,bridge=vmbr0,firewall=1'
        """
        parts: list[str] = []
        
        
        if self.bridge is not None:
            parts.append(f"bridge={self.bridge}")
        
        if self.firewall is not None:
            parts.append(f"firewall={self.firewall}")
        
        if self.gw is not None:
            parts.append(f"gw={self.gw}")
        
        if self.gw6 is not None:
            parts.append(f"gw6={self.gw6}")
        
        if self.host_managed is not None:
            parts.append(f"host-managed={self.host_managed}")
        
        if self.hwaddr is not None:
            parts.append(f"hwaddr={self.hwaddr}")
        
        if self.ip is not None:
            parts.append(f"ip={self.ip}")
        
        if self.ip6 is not None:
            parts.append(f"ip6={self.ip6}")
        
        if self.link_down is not None:
            parts.append(f"link_down={self.link_down}")
        
        if self.mtu is not None:
            parts.append(f"mtu={self.mtu}")
        
        if self.name is not None:
            parts.append(f"name={self.name}")
        
        if self.rate is not None:
            parts.append(f"rate={self.rate}")
        
        if self.tag is not None:
            parts.append(f"tag={self.tag}")
        
        if self.trunks is not None:
            parts.append(f"trunks={self.trunks}")
        
        if self.type is not None:
            parts.append(f"type={self.type}")
        
        return ",".join(parts)

    @classmethod
    def from_shorthand(cls, raw: Optional[str]) -> Optional["PveNetConfig"]:
        """Parse a Proxmox shorthand string into a PveNetConfig. Mirror of
        :meth:`to_shorthand`. The first comma-separated segment without
        ``=`` is interpreted as the default-key value.
        """
        if raw is None:
            return None
        kwargs: dict = {}
        default_key_consumed = False
        for part in raw.split(","):
            if not part:
                continue
            if "=" not in part:
                
                continue
            k, _, v = part.partition("=")
            kwargs[k.replace("-", "_")] = v
        return cls(**kwargs)

