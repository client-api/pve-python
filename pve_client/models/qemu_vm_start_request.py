# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 8.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from pve_client.models.pve_boolean import PveBoolean
from pve_client.models.pve_machine_field import PveMachineField
from pve_client.models.pve_migration_type_enum import PveMigrationTypeEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class QemuVmStartRequest(BaseModel):
    """
    QemuVmStartRequest
    """ # noqa: E501

    force_cpu: Optional[StrictStr] = Field(default=None, description="Override QEMU's -cpu argument with the given string.", alias="force-cpu")

    machine: Optional[PveMachineField] = Field(default=None, description="Specify the QEMU machine.")

    migratedfrom: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The cluster node name.")

    migration_network: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="CIDR of the (sub) network that is used for migration.")

    migration_type: Optional[PveMigrationTypeEnum] = Field(default=None, description="Migration traffic is encrypted using an SSH tunnel by default. On secure, completely private networks this can be disabled to increase performance.")

    nets_host_mtu: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Used for migration compat. List of VirtIO network devices and their effective host_mtu setting according to the QEMU object model on the source side of the migration. A value of 0 means that the host_mtu parameter is to be avoided for the corresponding device.", alias="nets-host-mtu")

    skiplock: Optional[PveBoolean] = Field(default=None, description="Ignore locks - only root is allowed to use this option.")

    stateuri: Optional[Annotated[str, Field(strict=True, max_length=128)]] = Field(default=None, description="Some command save/restore state from this location.")

    targetstorage: Optional[StrictStr] = Field(default=None, description="Mapping from source to target storages. Providing only a single storage ID maps all source storages to that storage. Providing the special value '1' will map each source storage to itself.")

    timeout: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="Wait maximal timeout seconds.")

    with_conntrack_state: Optional[PveBoolean] = Field(default=None, description="Whether to migrate conntrack entries for running VMs.", alias="with-conntrack-state")

    __properties: ClassVar[List[str]] = ["force-cpu", "machine", "migratedfrom", "migration_network", "migration_type", "nets-host-mtu", "skiplock", "stateuri", "targetstorage", "timeout", "with-conntrack-state"]




    @field_validator('migratedfrom')
    def migratedfrom_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(?:[a-zA-Z0-9](?:[a-zA-Z0-9\-]*[a-zA-Z0-9])?)$", value):
            raise ValueError(r"must validate the regular expression /^(?:[a-zA-Z0-9](?:[a-zA-Z0-9\-]*[a-zA-Z0-9])?)$/")
        return value


    @field_validator('migration_network')
    def migration_network_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(?:(?:[0-9a-fA-F]{0,4}:){2,7}[0-9a-fA-F]{0,4}\/[0-9]+|(?:[0-9]{1,3}\.){3}[0-9]{1,3}\/[0-9]{1,2})$", value):
            raise ValueError(r"must validate the regular expression /^(?:(?:[0-9a-fA-F]{0,4}:){2,7}[0-9a-fA-F]{0,4}\/[0-9]+|(?:[0-9]{1,3}\.){3}[0-9]{1,3}\/[0-9]{1,2})$/")
        return value



    @field_validator('nets_host_mtu')
    def nets_host_mtu_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"net\d+=\d+(,net\d+=\d+)*", value):
            raise ValueError(r"must validate the regular expression /net\d+=\d+(,net\d+=\d+)*/")
        return value








    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of QemuVmStartRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of machine
        if self.machine:
            _dict['machine'] = self.machine.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of QemuVmStartRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "force-cpu": obj.get("force-cpu"),
            "machine": PveMachineField.from_dict(obj["machine"]) if obj.get("machine") is not None else None,
            "migratedfrom": obj.get("migratedfrom"),
            "migration_network": obj.get("migration_network"),
            "migration_type": obj.get("migration_type"),
            "nets-host-mtu": obj.get("nets-host-mtu"),
            "skiplock": obj.get("skiplock"),
            "stateuri": obj.get("stateuri"),
            "targetstorage": obj.get("targetstorage"),
            "timeout": obj.get("timeout"),
            "with-conntrack-state": obj.get("with-conntrack-state")
        })
        return _obj


