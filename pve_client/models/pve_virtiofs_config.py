# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 8.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from pve_client.models.pve_boolean import PveBoolean
from pve_client.models.pve_qemu_cache_enum import PveQemuCacheEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PveVirtiofsConfig(BaseModel):
    """
    PveVirtiofsConfig
    """ # noqa: E501

    cache: Optional[PveQemuCacheEnum] = Field(default=None, description="The caching policy the file system should use (auto, always, metadata, never).")

    direct_io: Optional[PveBoolean] = Field(default=None, description="Honor the O_DIRECT flag passed down by guest applications.", alias="direct-io")

    dirid: Annotated[str, Field(strict=True)] = Field(description="Mapping identifier of the directory mapping to be shared with the guest. Also used as a mount tag inside the VM.")

    expose_acl: Optional[PveBoolean] = Field(default=None, description="Enable support for POSIX ACLs (enabled ACL implies xattr) for this mount.", alias="expose-acl")

    expose_xattr: Optional[PveBoolean] = Field(default=None, description="Enable support for extended attributes for this mount.", alias="expose-xattr")

    __properties: ClassVar[List[str]] = ["cache", "direct-io", "dirid", "expose-acl", "expose-xattr"]




    @field_validator('dirid')
    def dirid_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[A-Za-z][A-Za-z0-9_]*$", value):
            raise ValueError(r"must validate the regular expression /^[A-Za-z][A-Za-z0-9_]*$/")
        return value





    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PveVirtiofsConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PveVirtiofsConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "cache": obj.get("cache"),
            "direct-io": obj.get("direct-io"),
            "dirid": obj.get("dirid"),
            "expose-acl": obj.get("expose-acl"),
            "expose-xattr": obj.get("expose-xattr")
        })
        return _obj


    def to_shorthand(self) -> str:
        """Serialise this PveVirtiofsConfig into Proxmox's CLI-style shorthand
        string (``key=value,…``).

        The property marked ``x-pve-default-key`` is emitted positionally
        without a ``key=`` prefix; aliases collapse multiple property
        names to the same wire key.

        Example::

            >>> PveVirtiofsConfig(model='virtio', bridge='vmbr0', firewall=1).to_shorthand()
            'virtio,bridge=vmbr0,firewall=1'
        """
        parts: list[str] = []
        
        if self.dirid is not None:
            parts.append(str(self.dirid))
        
        
        if self.cache is not None:
            parts.append(f"cache={self.cache}")
        
        if self.direct_io is not None:
            parts.append(f"direct-io={self.direct_io}")
        
        if self.expose_acl is not None:
            parts.append(f"expose-acl={self.expose_acl}")
        
        if self.expose_xattr is not None:
            parts.append(f"expose-xattr={self.expose_xattr}")
        
        return ",".join(parts)

    @classmethod
    def from_shorthand(cls, raw: Optional[str]) -> Optional["PveVirtiofsConfig"]:
        """Parse a Proxmox shorthand string into a PveVirtiofsConfig. Mirror of
        :meth:`to_shorthand`. The first comma-separated segment without
        ``=`` is interpreted as the default-key value.
        """
        if raw is None:
            return None
        kwargs: dict = {}
        default_key_consumed = False
        for part in raw.split(","):
            if not part:
                continue
            if "=" not in part:
                
                if not default_key_consumed:
                    kwargs["dirid"] = part
                    default_key_consumed = True
                    continue
                
                continue
            k, _, v = part.partition("=")
            kwargs[k.replace("-", "_")] = v
        return cls(**kwargs)

