# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing_extensions import Annotated
from pve_client.models.pve_boolean import PveBoolean
from pve_client.models.pve_viommu_enum import PveViommuEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PveMachineConfig(BaseModel):
    """
    PveMachineConfig
    """ # noqa: E501

    aw_bits: Optional[Union[Annotated[float, Field(le=64, strict=True, ge=32)], Annotated[int, Field(le=64, strict=True, ge=32)]]] = Field(default=None, description="Specifies the vIOMMU address space bit width.  Intel vIOMMU supports a bit width of either 39 or 48 bits and VirtIO vIOMMU supports any bit width between 32 and 64 bits.", alias="aw-bits")

    enable_s3: Optional[PveBoolean] = Field(default=None, description="Enables S3 power state. Defaults to false beginning with machine types 9.2+pve1, true before.", alias="enable-s3")

    enable_s4: Optional[PveBoolean] = Field(default=None, description="Enables S4 power state. Defaults to false beginning with machine types 9.2+pve1, true before.", alias="enable-s4")

    type: Optional[Annotated[str, Field(strict=True, max_length=40)]] = Field(default=None, description="Specifies the QEMU machine type.")

    viommu: Optional[PveViommuEnum] = Field(default=None, description="Enable and set guest vIOMMU variant (Intel vIOMMU needs q35 to be set as machine type).")

    __properties: ClassVar[List[str]] = ["aw-bits", "enable-s3", "enable-s4", "type", "viommu"]





    @field_validator('type')
    def type_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"(pc|pc(-i440fx)?-\d+(\.\d+)+(\+pve\d+)?(\.pxe)?|q35|pc-q35-\d+(\.\d+)+(\+pve\d+)?(\.pxe)?|virt(?:-\d+(\.\d+)+)?(\+pve\d+)?)", value):
            raise ValueError(r"must validate the regular expression /(pc|pc(-i440fx)?-\d+(\.\d+)+(\+pve\d+)?(\.pxe)?|q35|pc-q35-\d+(\.\d+)+(\+pve\d+)?(\.pxe)?|virt(?:-\d+(\.\d+)+)?(\+pve\d+)?)/")
        return value




    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PveMachineConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PveMachineConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "aw-bits": obj.get("aw-bits"),
            "enable-s3": obj.get("enable-s3"),
            "enable-s4": obj.get("enable-s4"),
            "type": obj.get("type"),
            "viommu": obj.get("viommu")
        })
        return _obj


    def to_shorthand(self) -> str:
        """Serialise this PveMachineConfig into Proxmox's CLI-style shorthand
        string (``key=value,…``).

        The property marked ``x-pve-default-key`` is emitted positionally
        without a ``key=`` prefix; aliases collapse multiple property
        names to the same wire key.

        Example::

            >>> PveMachineConfig(model='virtio', bridge='vmbr0', firewall=1).to_shorthand()
            'virtio,bridge=vmbr0,firewall=1'
        """
        parts: list[str] = []
        
        if self.type is not None:
            parts.append(str(self.type))
        
        
        if self.aw_bits is not None:
            parts.append(f"aw-bits={self.aw_bits}")
        
        if self.enable_s3 is not None:
            parts.append(f"enable-s3={self.enable_s3}")
        
        if self.enable_s4 is not None:
            parts.append(f"enable-s4={self.enable_s4}")
        
        if self.viommu is not None:
            parts.append(f"viommu={self.viommu}")
        
        return ",".join(parts)

    @classmethod
    def from_shorthand(cls, raw: Optional[str]) -> Optional["PveMachineConfig"]:
        """Parse a Proxmox shorthand string into a PveMachineConfig. Mirror of
        :meth:`to_shorthand`. The first comma-separated segment without
        ``=`` is interpreted as the default-key value.
        """
        if raw is None:
            return None
        kwargs: dict = {}
        default_key_consumed = False
        for part in raw.split(","):
            if not part:
                continue
            if "=" not in part:
                
                if not default_key_consumed:
                    kwargs["type"] = part
                    default_key_consumed = True
                    continue
                
                continue
            k, _, v = part.partition("=")
            kwargs[k.replace("-", "_")] = v
        return cls(**kwargs)

