# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 8.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PveWakeonlanConfig(BaseModel):
    """
    PveWakeonlanConfig
    """ # noqa: E501

    bind_interface: Optional[Annotated[str, Field(strict=True)]] = Field(default='The interface carrying the default route', description="Bind to this interface when sending wake on LAN packet", alias="bind-interface")

    broadcast_address: Optional[Annotated[str, Field(strict=True)]] = Field(default='255.255.255.255', description="IPv4 broadcast address to use when sending wake on LAN packet", alias="broadcast-address")

    mac: Annotated[str, Field(strict=True)] = Field(description="MAC address for wake on LAN")

    __properties: ClassVar[List[str]] = ["bind-interface", "broadcast-address", "mac"]


    @field_validator('bind_interface')
    def bind_interface_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[a-zA-Z][a-zA-Z0-9_]{1,20}$", value):
            raise ValueError(r"must validate the regular expression /^[a-zA-Z][a-zA-Z0-9_]{1,20}$/")
        return value


    @field_validator('broadcast_address')
    def broadcast_address_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$", value):
            raise ValueError(r"must validate the regular expression /^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$/")
        return value


    @field_validator('mac')
    def mac_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[0-9a-fA-F]{2}(?:[:\-][0-9a-fA-F]{2}){5}$", value):
            raise ValueError(r"must validate the regular expression /^[0-9a-fA-F]{2}(?:[:\-][0-9a-fA-F]{2}){5}$/")
        return value



    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PveWakeonlanConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PveWakeonlanConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "bind-interface": obj.get("bind-interface") if obj.get("bind-interface") is not None else 'The interface carrying the default route',
            "broadcast-address": obj.get("broadcast-address") if obj.get("broadcast-address") is not None else '255.255.255.255',
            "mac": obj.get("mac")
        })
        return _obj


    def to_shorthand(self) -> str:
        """Serialise this PveWakeonlanConfig into Proxmox's CLI-style shorthand
        string (``key=value,…``).

        The property marked ``x-pve-default-key`` is emitted positionally
        without a ``key=`` prefix; aliases collapse multiple property
        names to the same wire key.

        Example::

            >>> PveWakeonlanConfig(model='virtio', bridge='vmbr0', firewall=1).to_shorthand()
            'virtio,bridge=vmbr0,firewall=1'
        """
        parts: list[str] = []
        
        if self.mac is not None:
            parts.append(str(self.mac))
        
        
        if self.bind_interface is not None:
            parts.append(f"bind-interface={self.bind_interface}")
        
        if self.broadcast_address is not None:
            parts.append(f"broadcast-address={self.broadcast_address}")
        
        return ",".join(parts)

    @classmethod
    def from_shorthand(cls, raw: Optional[str]) -> Optional["PveWakeonlanConfig"]:
        """Parse a Proxmox shorthand string into a PveWakeonlanConfig. Mirror of
        :meth:`to_shorthand`. The first comma-separated segment without
        ``=`` is interpreted as the default-key value.
        """
        if raw is None:
            return None
        kwargs: dict = {}
        default_key_consumed = False
        for part in raw.split(","):
            if not part:
                continue
            if "=" not in part:
                
                if not default_key_consumed:
                    kwargs["mac"] = part
                    default_key_consumed = True
                    continue
                
                continue
            k, _, v = part.partition("=")
            kwargs[k.replace("-", "_")] = v
        return cls(**kwargs)

