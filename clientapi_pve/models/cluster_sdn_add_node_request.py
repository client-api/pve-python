# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from clientapi_pve.models.cluster_sdn_list_all_response_data_nodes_inner_peers_inner import ClusterSdnListAllResponseDataNodesInnerPeersInner
from clientapi_pve.models.pve_protocol_enum import PveProtocolEnum
from clientapi_pve.models.pve_role_enum import PveRoleEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ClusterSdnAddNodeRequest(BaseModel):
    """
    ClusterSdnAddNodeRequest
    """ # noqa: E501

    allowed_ips: Optional[List[StrictStr]] = Field(default=None, description="A list of IPs that are routable via this node in the WireGuard fabric.")

    digest: Optional[Annotated[str, Field(strict=True, max_length=64)]] = Field(default=None, description="Prevent changes if current configuration file has a different digest. This can be used to prevent concurrent modifications.")

    endpoint: Optional[StrictStr] = Field(default=None, description="The endpoint used for connecting to this node.")

    interfaces: List[Any]

    ip: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="IPv4 address for this node")

    ip6: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="IPv6 address for this node")

    lock_token: Optional[StrictStr] = Field(default=None, description="the token for unlocking the global SDN configuration", alias="lock-token")

    node_id: Annotated[str, Field(strict=True)] = Field(description="Identifier for nodes in an SDN fabric")

    peers: Optional[List[ClusterSdnListAllResponseDataNodesInnerPeersInner]] = None

    protocol: PveProtocolEnum = Field(description="Type of configuration entry in an SDN Fabric section config")

    public_key: Optional[StrictStr] = Field(default=None, description="The public key for the external node.")

    role: Optional[PveRoleEnum] = Field(default=None, description="The role of this node in the WireGuard fabric.")

    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["allowed_ips", "digest", "endpoint", "interfaces", "ip", "ip6", "lock-token", "node_id", "peers", "protocol", "public_key", "role"]






    @field_validator('ip')
    def ip_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$", value):
            raise ValueError(r"must validate the regular expression /^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$/")
        return value


    @field_validator('ip6')
    def ip6_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(?:[0-9a-fA-F]{0,4}:){2,7}[0-9a-fA-F]{0,4}$", value):
            raise ValueError(r"must validate the regular expression /^(?:[0-9a-fA-F]{0,4}:){2,7}[0-9a-fA-F]{0,4}$/")
        return value



    @field_validator('node_id')
    def node_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(?:[a-zA-Z0-9](?:[a-zA-Z0-9\-]*[a-zA-Z0-9])?)$", value):
            raise ValueError(r"must validate the regular expression /^(?:[a-zA-Z0-9](?:[a-zA-Z0-9\-]*[a-zA-Z0-9])?)$/")
        return value







    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ClusterSdnAddNodeRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            # `exclude_unset` keeps schema defaults out of the wire payload
            # when the user constructed the model directly (e.g.
            # `Req(vmid=100)` would otherwise pull in
            # `cores=1, cpulimit=0, …` from the spec defaults and PVE
            # rejects the request with 400 because it never set those).
            # `exclude_none` keeps None values out of the wire payload —
            # both for direct construction (None means "unset") and for
            # the from_dict path (where unspecified obj keys become
            # `obj.get("k") == None` but show up in `model_fields_set`).
            exclude_unset=True,
            exclude_none=True,
        )
        
        
        
        
        
        
        
        
        
        # override the default output from pydantic by calling `to_dict()` of each item in peers (list)
        _items = []
        if self.peers:
            for _item_peers in self.peers:
                if _item_peers:
                    _items.append(_item_peers.to_dict())
            _dict['peers'] = _items
        
        
        
        
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        
        
        
        
        
        
        
        
        
        
        
        
        
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ClusterSdnAddNodeRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "allowed_ips": obj.get("allowed_ips"),
            "digest": obj.get("digest"),
            "endpoint": obj.get("endpoint"),
            "interfaces": obj.get("interfaces"),
            "ip": obj.get("ip"),
            "ip6": obj.get("ip6"),
            "lock-token": obj.get("lock-token"),
            "node_id": obj.get("node_id"),
            "peers": [ClusterSdnListAllResponseDataNodesInnerPeersInner.from_dict(_item) for _item in obj["peers"]] if obj.get("peers") is not None else None,
            "protocol": obj.get("protocol"),
            "public_key": obj.get("public_key"),
            "role": obj.get("role")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


