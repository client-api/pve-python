# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from clientapi_pve.models.pve_boolean import PveBoolean
from clientapi_pve.models.pve_qemu_type_enum import PveQemuTypeEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PveAgentConfig(BaseModel):
    """
    PveAgentConfig
    """ # noqa: E501

    enabled: PveBoolean = Field(description="Enable/disable communication with a QEMU Guest Agent (QGA) running in the VM.")

    freeze_fs: Optional[PveBoolean] = Field(default=None, description="Whether to issue the guest-fsfreeze-freeze and guest-fsfreeze-thaw QEMU guest agent commands. Backups in snapshot mode, clones, snapshots without RAM, importing disks from a running guest, and replications normally issue a guest-fsfreeze-freeze and a respective thaw command when the QEMU Guest agent option is enabled in the guest's configuration and the agent is running inside of the guest.  The deprecated 'freeze-fs-on-backup' setting is treated as an alias for this setting.", alias="freeze-fs")

    freeze_fs_on_backup: Optional[StrictStr] = Field(default=None, alias="freeze-fs-on-backup")

    fstrim_cloned_disks: Optional[PveBoolean] = Field(default=None, description="Run fstrim after moving a disk or migrating the VM.")

    guest_fsfreeze: Optional[StrictStr] = Field(default=None, alias="guest-fsfreeze")

    type: Optional[PveQemuTypeEnum] = Field(default=None, description="Select the agent type")

    __properties: ClassVar[List[str]] = ["enabled", "freeze-fs", "freeze-fs-on-backup", "fstrim_cloned_disks", "guest-fsfreeze", "type"]









    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PveAgentConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            # `exclude_unset` keeps schema defaults out of the wire payload
            # when the user constructed the model directly (e.g.
            # `Req(vmid=100)` would otherwise pull in
            # `cores=1, cpulimit=0, …` from the spec defaults and PVE
            # rejects the request with 400 because it never set those).
            # `exclude_none` keeps None values out of the wire payload —
            # both for direct construction (None means "unset") and for
            # the from_dict path (where unspecified obj keys become
            # `obj.get("k") == None` but show up in `model_fields_set`).
            exclude_unset=True,
            exclude_none=True,
        )
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PveAgentConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "enabled": obj.get("enabled"),
            "freeze-fs": obj.get("freeze-fs"),
            "freeze-fs-on-backup": obj.get("freeze-fs-on-backup"),
            "fstrim_cloned_disks": obj.get("fstrim_cloned_disks"),
            "guest-fsfreeze": obj.get("guest-fsfreeze"),
            "type": obj.get("type")
        })
        return _obj


    def to_shorthand(self) -> str:
        """Serialise this PveAgentConfig into Proxmox's CLI-style shorthand
        string (``key=value,…``).

        The property marked ``x-pve-default-key`` is emitted positionally
        without a ``key=`` prefix; aliases collapse multiple property
        names to the same wire key.

        Example::

            >>> PveAgentConfig(model='virtio', bridge='vmbr0', firewall=1).to_shorthand()
            'virtio,bridge=vmbr0,firewall=1'
        """
        parts: list[str] = []
        
        if self.enabled is not None:
            parts.append(str(self.enabled))
        
        
        if self.freeze_fs is not None:
            parts.append(f"freeze-fs={self.freeze_fs}")
        
        if self.freeze_fs_on_backup is not None:
            parts.append(f"freeze-fs={self.freeze_fs_on_backup}")
        
        if self.fstrim_cloned_disks is not None:
            parts.append(f"fstrim_cloned_disks={self.fstrim_cloned_disks}")
        
        if self.guest_fsfreeze is not None:
            parts.append(f"freeze-fs={self.guest_fsfreeze}")
        
        if self.type is not None:
            parts.append(f"type={self.type}")
        
        return ",".join(parts)

    @classmethod
    def from_shorthand(cls, raw: Optional[str]) -> Optional["PveAgentConfig"]:
        """Parse a Proxmox shorthand string into a PveAgentConfig. Mirror of
        :meth:`to_shorthand`. The first comma-separated segment without
        ``=`` is interpreted as the default-key value.
        """
        if raw is None:
            return None
        kwargs: dict = {}
        default_key_consumed = False
        for part in raw.split(","):
            if not part:
                continue
            if "=" not in part:
                
                if not default_key_consumed:
                    kwargs["enabled"] = part
                    default_key_consumed = True
                    continue
                
                continue
            k, _, v = part.partition("=")
            kwargs[k.replace("-", "_")] = v
        return cls(**kwargs)

