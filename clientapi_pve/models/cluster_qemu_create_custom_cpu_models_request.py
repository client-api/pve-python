# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from clientapi_pve.models.pve_boolean import PveBoolean
from clientapi_pve.models.pve_reported_model_enum import PveReportedModelEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ClusterQemuCreateCustomCpuModelsRequest(BaseModel):
    """
    ClusterQemuCreateCustomCpuModelsRequest
    """ # noqa: E501

    cputype: Annotated[str, Field(strict=True, max_length=40)] = Field(description="Name for the custom CPU model. The 'custom-' prefix is optional.")

    flags: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="List of additional CPU flags separated by ';'. Use '+FLAG' to enable, '-FLAG' to disable a flag. There is a special 'nested-virt' shorthand which controls nested virtualization for the current CPU ('svm' for AMD and 'vmx' for Intel). Custom CPU models can specify any flag supported by QEMU/KVM, VM-specific flags must be from the following set for security reasons: aes, amd-no-ssb, amd-ssbd, hv-evmcs, hv-tlbflush, ibpb, md-clear, nested-virt, pcid, pdpe1gb, spec-ctrl, ssbd, virt-ssbd")

    guest_phys_bits: Optional[Annotated[int, Field(le=64, strict=True, ge=32)]] = Field(default=None, description="Number of physical address bits available to the guest.", alias="guest-phys-bits")

    hidden: Optional[PveBoolean] = Field(default=None, description="Do not identify as a KVM virtual machine. Only affects vCPUs with x86-64 architecture.")

    hv_vendor_id: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The Hyper-V vendor ID. Some drivers or programs inside Windows guests need a specific ID.", alias="hv-vendor-id")

    level: Optional[Annotated[int, Field(le=4294967295, strict=True, ge=0)]] = Field(default=None, description="Maximum input value for the basic CPUID leaves the guest can query - that is the vendor (leaf 0), family/model/stepping and feature bits (leaf 1), cache and topology info (leaves 4 and B), and so on. Higher-numbered leaves are hidden. Setting '30' is a common workaround for Hyper-V boot failures on Windows guests running on recent Intel hosts. Only applies when the vCPU architecture is x86_64.")

    phys_bits: Optional[StrictStr] = Field(default=None, description="The physical memory address bits that are reported to the guest OS. Should be smaller or equal to the host's. Set to 'host' to use value from host CPU, but note that doing so will break live migration to CPUs with other values.", alias="phys-bits")

    reported_model: PveReportedModelEnum = Field(description="CPU model and vendor to report to the guest. Must be a QEMU/KVM supported model. Only valid for custom CPU model definitions, default models will always report themselves to the guest OS.", alias="reported-model")

    __properties: ClassVar[List[str]] = ["cputype", "flags", "guest-phys-bits", "hidden", "hv-vendor-id", "level", "phys-bits", "reported-model"]


    @field_validator('cputype')
    def cputype_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[A-Za-z][A-Za-z0-9_]*$", value):
            raise ValueError(r"must validate the regular expression /^[A-Za-z][A-Za-z0-9_]*$/")
        return value


    @field_validator('flags')
    def flags_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"(?:(?:([+-])([a-zA-Z0-9\-_\.]+))(;(?:([+-])([a-zA-Z0-9\-_\.]+)))*)", value):
            raise ValueError(r"must validate the regular expression /(?:(?:([+-])([a-zA-Z0-9\-_\.]+))(;(?:([+-])([a-zA-Z0-9\-_\.]+)))*)/")
        return value




    @field_validator('hv_vendor_id')
    def hv_vendor_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"(?:[a-zA-Z0-9]{1,12})", value):
            raise ValueError(r"must validate the regular expression /(?:[a-zA-Z0-9]{1,12})/")
        return value






    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ClusterQemuCreateCustomCpuModelsRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            # `exclude_unset` keeps schema defaults out of the wire payload
            # when the user constructed the model directly (e.g.
            # `Req(vmid=100)` would otherwise pull in
            # `cores=1, cpulimit=0, …` from the spec defaults and PVE
            # rejects the request with 400 because it never set those).
            # `exclude_none` keeps None values out of the wire payload —
            # both for direct construction (None means "unset") and for
            # the from_dict path (where unspecified obj keys become
            # `obj.get("k") == None` but show up in `model_fields_set`).
            exclude_unset=True,
            exclude_none=True,
        )
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ClusterQemuCreateCustomCpuModelsRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "cputype": obj.get("cputype"),
            "flags": obj.get("flags"),
            "guest-phys-bits": obj.get("guest-phys-bits"),
            "hidden": obj.get("hidden"),
            "hv-vendor-id": obj.get("hv-vendor-id"),
            "level": obj.get("level"),
            "phys-bits": obj.get("phys-bits"),
            "reported-model": obj.get("reported-model")
        })
        return _obj


