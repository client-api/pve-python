# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from clientapi_pve.models.pve_boolean import PveBoolean
from clientapi_pve.models.pve_state_enum import PveStateEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ClusterHaReadGetResourcesResponseData(BaseModel):
    """
    ClusterHaReadGetResourcesResponseData
    """ # noqa: E501

    auto_rebalance: Optional[PveBoolean] = Field(default=None, description="HA resource may be migrated during automatic rebalancing.", alias="auto-rebalance")

    comment: Optional[StrictStr] = Field(default=None, description="Description.")

    digest: StrictStr = Field(description="Can be used to prevent concurrent modifications.")

    failback: Optional[PveBoolean] = Field(default=None, description="The HA resource is automatically migrated to the node with the highest priority according to their node affinity rule, if a node with a higher priority than the current node comes online.")

    group: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The HA group identifier.")

    max_relocate: Optional[int] = Field(default=None, description="Maximal number of service relocate tries when a service fails to start.")

    max_restart: Optional[int] = Field(default=None, description="Maximal number of tries to restart the service on a node after its start failed.")

    sid: StrictStr = Field(description="HA resource ID. This consists of a resource type followed by a resource specific name, separated with colon (example: vm:100 / ct:100). For virtual machines and containers, you can simply use the VM or CT id as a shortcut (example: 100).")

    state: Optional[PveStateEnum] = Field(default=None, description="Requested resource state.")

    type: StrictStr = Field(description="The type of the resources.")

    __properties: ClassVar[List[str]] = ["auto-rebalance", "comment", "digest", "failback", "group", "max_relocate", "max_restart", "sid", "state", "type"]






    @field_validator('group')
    def group_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[A-Za-z][A-Za-z0-9_]*$", value):
            raise ValueError(r"must validate the regular expression /^[A-Za-z][A-Za-z0-9_]*$/")
        return value








    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ClusterHaReadGetResourcesResponseData from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            # `exclude_unset` keeps schema defaults out of the wire payload
            # when the user constructed the model directly (e.g.
            # `Req(vmid=100)` would otherwise pull in
            # `cores=1, cpulimit=0, …` from the spec defaults and PVE
            # rejects the request with 400 because it never set those).
            # `exclude_none` keeps None values out of the wire payload —
            # both for direct construction (None means "unset") and for
            # the from_dict path (where unspecified obj keys become
            # `obj.get("k") == None` but show up in `model_fields_set`).
            exclude_unset=True,
            exclude_none=True,
        )
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ClusterHaReadGetResourcesResponseData from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "auto-rebalance": obj.get("auto-rebalance"),
            "comment": obj.get("comment"),
            "digest": obj.get("digest"),
            "failback": obj.get("failback"),
            "group": obj.get("group"),
            "max_relocate": obj.get("max_relocate"),
            "max_restart": obj.get("max_restart"),
            "sid": obj.get("sid"),
            "state": obj.get("state"),
            "type": obj.get("type")
        })
        return _obj


