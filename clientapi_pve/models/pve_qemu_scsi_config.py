# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing_extensions import Annotated
from clientapi_pve.models.pve_aio_enum import PveAioEnum
from clientapi_pve.models.pve_boolean import PveBoolean
from clientapi_pve.models.pve_cache_enum import PveCacheEnum
from clientapi_pve.models.pve_discard_enum import PveDiscardEnum
from clientapi_pve.models.pve_format_enum import PveFormatEnum
from clientapi_pve.models.pve_media_enum import PveMediaEnum
from clientapi_pve.models.pve_rerror_enum import PveRerrorEnum
from clientapi_pve.models.pve_werror_enum import PveWerrorEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PveQemuScsiConfig(BaseModel):
    """
    PveQemuScsiConfig
    """ # noqa: E501

    aio: Optional[PveAioEnum] = Field(default=None, description="AIO type to use.")

    backup: Optional[PveBoolean] = Field(default=None, description="Whether the drive should be included when making backups.")

    bps: Optional[StrictInt] = Field(default=None, description="Maximum r/w speed in bytes per second.")

    bps_max_length: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=None, description="Maximum length of I/O bursts in seconds.")

    bps_rd: Optional[StrictInt] = Field(default=None, description="Maximum read speed in bytes per second.")

    bps_rd_length: Optional[StrictStr] = None

    bps_rd_max_length: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=None, description="Maximum length of read I/O bursts in seconds.")

    bps_wr: Optional[StrictInt] = Field(default=None, description="Maximum write speed in bytes per second.")

    bps_wr_length: Optional[StrictStr] = None

    bps_wr_max_length: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=None, description="Maximum length of write I/O bursts in seconds.")

    cache: Optional[PveCacheEnum] = Field(default=None, description="The drive's cache mode")

    detect_zeroes: Optional[PveBoolean] = Field(default=None, description="Controls whether to detect and try to optimize writes of zeroes.")

    discard: Optional[PveDiscardEnum] = Field(default=None, description="Controls whether to pass discard/trim requests to the underlying storage.")

    file: Annotated[str, Field(strict=True)] = Field(description="The drive's backing volume.")

    format: Optional[PveFormatEnum] = Field(default=None, description="The drive's backing file's data format.")

    iops: Optional[StrictInt] = Field(default=None, description="Maximum r/w I/O in operations per second.")

    iops_max: Optional[StrictInt] = Field(default=None, description="Maximum unthrottled r/w I/O pool in operations per second.")

    iops_max_length: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=None, description="Maximum length of I/O bursts in seconds.")

    iops_rd: Optional[StrictInt] = Field(default=None, description="Maximum read I/O in operations per second.")

    iops_rd_length: Optional[StrictStr] = None

    iops_rd_max: Optional[StrictInt] = Field(default=None, description="Maximum unthrottled read I/O pool in operations per second.")

    iops_rd_max_length: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=None, description="Maximum length of read I/O bursts in seconds.")

    iops_wr: Optional[StrictInt] = Field(default=None, description="Maximum write I/O in operations per second.")

    iops_wr_length: Optional[StrictStr] = None

    iops_wr_max: Optional[StrictInt] = Field(default=None, description="Maximum unthrottled write I/O pool in operations per second.")

    iops_wr_max_length: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=None, description="Maximum length of write I/O bursts in seconds.")

    iothread: Optional[PveBoolean] = Field(default=None, description="Whether to use iothreads for this drive")

    mbps: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Maximum r/w speed in megabytes per second.")

    mbps_max: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Maximum unthrottled r/w pool in megabytes per second.")

    mbps_rd: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Maximum read speed in megabytes per second.")

    mbps_rd_max: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Maximum unthrottled read pool in megabytes per second.")

    mbps_wr: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Maximum write speed in megabytes per second.")

    mbps_wr_max: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Maximum unthrottled write pool in megabytes per second.")

    media: Optional[PveMediaEnum] = Field(default=None, description="The drive's media type.")

    product: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The drive's product name, up to 16 bytes long.")

    queues: Optional[Annotated[int, Field(strict=True, ge=2)]] = Field(default=None, description="Number of queues.")

    replicate: Optional[PveBoolean] = Field(default=None, description="Whether the drive should considered for replication jobs.")

    rerror: Optional[PveRerrorEnum] = Field(default=None, description="Read error action.")

    ro: Optional[PveBoolean] = Field(default=None, description="Whether the drive is read-only.")

    scsiblock: Optional[PveBoolean] = Field(default=None, description="whether to use scsi-block for full passthrough of host block device  WARNING: can lead to I/O errors in combination with low memory or high memory fragmentation on host")

    serial: Optional[Annotated[str, Field(strict=True, max_length=60)]] = Field(default=None, description="The drive's reported serial number, url-encoded, up to 20 bytes long.")

    shared: Optional[PveBoolean] = Field(default=None, description="Mark this locally-managed volume as available on all nodes.  WARNING: This option does not share the volume automatically, it assumes it is shared already!")

    size: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Disk size. This is purely informational and has no effect.")

    snapshot: Optional[PveBoolean] = Field(default=None, description="Controls qemu's snapshot mode feature. If activated, changes made to the disk are temporary and will be discarded when the VM is shutdown.")

    ssd: Optional[PveBoolean] = Field(default=None, description="Whether to expose this drive as an SSD, rather than a rotational hard disk.")

    vendor: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The drive's vendor name, up to 8 bytes long.")

    volume: Optional[StrictStr] = None

    werror: Optional[PveWerrorEnum] = Field(default=None, description="Write error action.")

    wwn: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The drive's worldwide name, encoded as 16 bytes hex string, prefixed by '0x'.")

    __properties: ClassVar[List[str]] = ["aio", "backup", "bps", "bps_max_length", "bps_rd", "bps_rd_length", "bps_rd_max_length", "bps_wr", "bps_wr_length", "bps_wr_max_length", "cache", "detect_zeroes", "discard", "file", "format", "iops", "iops_max", "iops_max_length", "iops_rd", "iops_rd_length", "iops_rd_max", "iops_rd_max_length", "iops_wr", "iops_wr_length", "iops_wr_max", "iops_wr_max_length", "iothread", "mbps", "mbps_max", "mbps_rd", "mbps_rd_max", "mbps_wr", "mbps_wr_max", "media", "product", "queues", "replicate", "rerror", "ro", "scsiblock", "serial", "shared", "size", "snapshot", "ssd", "vendor", "volume", "werror", "wwn"]















    @field_validator('file')
    def file_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(?:[A-Za-z0-9_\-]+:.+|\/.+)$", value):
            raise ValueError(r"must validate the regular expression /^(?:[A-Za-z0-9_\-]+:.+|\/.+)$/")
        return value






















    @field_validator('product')
    def product_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"[A-Za-z0-9\-_\s]{,16}", value):
            raise ValueError(r"must validate the regular expression /[A-Za-z0-9\-_\s]{,16}/")
        return value









    @field_validator('size')
    def size_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^\d+(?:\.\d+)?[KMGT]?$", value):
            raise ValueError(r"must validate the regular expression /^\d+(?:\.\d+)?[KMGT]?$/")
        return value




    @field_validator('vendor')
    def vendor_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"[A-Za-z0-9\-_\s]{,8}", value):
            raise ValueError(r"must validate the regular expression /[A-Za-z0-9\-_\s]{,8}/")
        return value




    @field_validator('wwn')
    def wwn_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"(?:^(0x)[0-9a-fA-F]{16})", value):
            raise ValueError(r"must validate the regular expression /(?:^(0x)[0-9a-fA-F]{16})/")
        return value



    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PveQemuScsiConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PveQemuScsiConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "aio": obj.get("aio"),
            "backup": obj.get("backup"),
            "bps": obj.get("bps"),
            "bps_max_length": obj.get("bps_max_length"),
            "bps_rd": obj.get("bps_rd"),
            "bps_rd_length": obj.get("bps_rd_length"),
            "bps_rd_max_length": obj.get("bps_rd_max_length"),
            "bps_wr": obj.get("bps_wr"),
            "bps_wr_length": obj.get("bps_wr_length"),
            "bps_wr_max_length": obj.get("bps_wr_max_length"),
            "cache": obj.get("cache"),
            "detect_zeroes": obj.get("detect_zeroes"),
            "discard": obj.get("discard"),
            "file": obj.get("file"),
            "format": obj.get("format"),
            "iops": obj.get("iops"),
            "iops_max": obj.get("iops_max"),
            "iops_max_length": obj.get("iops_max_length"),
            "iops_rd": obj.get("iops_rd"),
            "iops_rd_length": obj.get("iops_rd_length"),
            "iops_rd_max": obj.get("iops_rd_max"),
            "iops_rd_max_length": obj.get("iops_rd_max_length"),
            "iops_wr": obj.get("iops_wr"),
            "iops_wr_length": obj.get("iops_wr_length"),
            "iops_wr_max": obj.get("iops_wr_max"),
            "iops_wr_max_length": obj.get("iops_wr_max_length"),
            "iothread": obj.get("iothread"),
            "mbps": obj.get("mbps"),
            "mbps_max": obj.get("mbps_max"),
            "mbps_rd": obj.get("mbps_rd"),
            "mbps_rd_max": obj.get("mbps_rd_max"),
            "mbps_wr": obj.get("mbps_wr"),
            "mbps_wr_max": obj.get("mbps_wr_max"),
            "media": obj.get("media"),
            "product": obj.get("product"),
            "queues": obj.get("queues"),
            "replicate": obj.get("replicate"),
            "rerror": obj.get("rerror"),
            "ro": obj.get("ro"),
            "scsiblock": obj.get("scsiblock"),
            "serial": obj.get("serial"),
            "shared": obj.get("shared"),
            "size": obj.get("size"),
            "snapshot": obj.get("snapshot"),
            "ssd": obj.get("ssd"),
            "vendor": obj.get("vendor"),
            "volume": obj.get("volume"),
            "werror": obj.get("werror"),
            "wwn": obj.get("wwn")
        })
        return _obj


    def to_shorthand(self) -> str:
        """Serialise this PveQemuScsiConfig into Proxmox's CLI-style shorthand
        string (``key=value,…``).

        The property marked ``x-pve-default-key`` is emitted positionally
        without a ``key=`` prefix; aliases collapse multiple property
        names to the same wire key.

        Example::

            >>> PveQemuScsiConfig(model='virtio', bridge='vmbr0', firewall=1).to_shorthand()
            'virtio,bridge=vmbr0,firewall=1'
        """
        parts: list[str] = []
        
        if self.file is not None:
            parts.append(str(self.file))
        
        
        if self.aio is not None:
            parts.append(f"aio={self.aio}")
        
        if self.backup is not None:
            parts.append(f"backup={self.backup}")
        
        if self.bps is not None:
            parts.append(f"bps={self.bps}")
        
        if self.bps_max_length is not None:
            parts.append(f"bps_max_length={self.bps_max_length}")
        
        if self.bps_rd is not None:
            parts.append(f"bps_rd={self.bps_rd}")
        
        if self.bps_rd_length is not None:
            parts.append(f"bps_rd_max_length={self.bps_rd_length}")
        
        if self.bps_rd_max_length is not None:
            parts.append(f"bps_rd_max_length={self.bps_rd_max_length}")
        
        if self.bps_wr is not None:
            parts.append(f"bps_wr={self.bps_wr}")
        
        if self.bps_wr_length is not None:
            parts.append(f"bps_wr_max_length={self.bps_wr_length}")
        
        if self.bps_wr_max_length is not None:
            parts.append(f"bps_wr_max_length={self.bps_wr_max_length}")
        
        if self.cache is not None:
            parts.append(f"cache={self.cache}")
        
        if self.detect_zeroes is not None:
            parts.append(f"detect_zeroes={self.detect_zeroes}")
        
        if self.discard is not None:
            parts.append(f"discard={self.discard}")
        
        if self.format is not None:
            parts.append(f"format={self.format}")
        
        if self.iops is not None:
            parts.append(f"iops={self.iops}")
        
        if self.iops_max is not None:
            parts.append(f"iops_max={self.iops_max}")
        
        if self.iops_max_length is not None:
            parts.append(f"iops_max_length={self.iops_max_length}")
        
        if self.iops_rd is not None:
            parts.append(f"iops_rd={self.iops_rd}")
        
        if self.iops_rd_length is not None:
            parts.append(f"iops_rd_max_length={self.iops_rd_length}")
        
        if self.iops_rd_max is not None:
            parts.append(f"iops_rd_max={self.iops_rd_max}")
        
        if self.iops_rd_max_length is not None:
            parts.append(f"iops_rd_max_length={self.iops_rd_max_length}")
        
        if self.iops_wr is not None:
            parts.append(f"iops_wr={self.iops_wr}")
        
        if self.iops_wr_length is not None:
            parts.append(f"iops_wr_max_length={self.iops_wr_length}")
        
        if self.iops_wr_max is not None:
            parts.append(f"iops_wr_max={self.iops_wr_max}")
        
        if self.iops_wr_max_length is not None:
            parts.append(f"iops_wr_max_length={self.iops_wr_max_length}")
        
        if self.iothread is not None:
            parts.append(f"iothread={self.iothread}")
        
        if self.mbps is not None:
            parts.append(f"mbps={self.mbps}")
        
        if self.mbps_max is not None:
            parts.append(f"mbps_max={self.mbps_max}")
        
        if self.mbps_rd is not None:
            parts.append(f"mbps_rd={self.mbps_rd}")
        
        if self.mbps_rd_max is not None:
            parts.append(f"mbps_rd_max={self.mbps_rd_max}")
        
        if self.mbps_wr is not None:
            parts.append(f"mbps_wr={self.mbps_wr}")
        
        if self.mbps_wr_max is not None:
            parts.append(f"mbps_wr_max={self.mbps_wr_max}")
        
        if self.media is not None:
            parts.append(f"media={self.media}")
        
        if self.product is not None:
            parts.append(f"product={self.product}")
        
        if self.queues is not None:
            parts.append(f"queues={self.queues}")
        
        if self.replicate is not None:
            parts.append(f"replicate={self.replicate}")
        
        if self.rerror is not None:
            parts.append(f"rerror={self.rerror}")
        
        if self.ro is not None:
            parts.append(f"ro={self.ro}")
        
        if self.scsiblock is not None:
            parts.append(f"scsiblock={self.scsiblock}")
        
        if self.serial is not None:
            parts.append(f"serial={self.serial}")
        
        if self.shared is not None:
            parts.append(f"shared={self.shared}")
        
        if self.size is not None:
            parts.append(f"size={self.size}")
        
        if self.snapshot is not None:
            parts.append(f"snapshot={self.snapshot}")
        
        if self.ssd is not None:
            parts.append(f"ssd={self.ssd}")
        
        if self.vendor is not None:
            parts.append(f"vendor={self.vendor}")
        
        if self.volume is not None:
            parts.append(f"file={self.volume}")
        
        if self.werror is not None:
            parts.append(f"werror={self.werror}")
        
        if self.wwn is not None:
            parts.append(f"wwn={self.wwn}")
        
        return ",".join(parts)

    @classmethod
    def from_shorthand(cls, raw: Optional[str]) -> Optional["PveQemuScsiConfig"]:
        """Parse a Proxmox shorthand string into a PveQemuScsiConfig. Mirror of
        :meth:`to_shorthand`. The first comma-separated segment without
        ``=`` is interpreted as the default-key value.
        """
        if raw is None:
            return None
        kwargs: dict = {}
        default_key_consumed = False
        for part in raw.split(","):
            if not part:
                continue
            if "=" not in part:
                
                if not default_key_consumed:
                    kwargs["file"] = part
                    default_key_consumed = True
                    continue
                
                continue
            k, _, v = part.partition("=")
            kwargs[k.replace("-", "_")] = v
        return cls(**kwargs)

