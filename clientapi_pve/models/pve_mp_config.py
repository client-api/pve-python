# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from clientapi_pve.models.pve_boolean import PveBoolean
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PveMpConfig(BaseModel):
    """
    PveMpConfig
    """ # noqa: E501

    acl: Optional[PveBoolean] = Field(default=None, description="Explicitly enable or disable ACL support.")

    backup: Optional[PveBoolean] = Field(default=None, description="Whether to include the mount point in backups (only used for volume mount points).")

    idmap: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Customize UID/GID mappings that override the container's `lxc.idmap` for this mount point. Accepts a semicolon-separated list of `type:container:disk:range-size` entries.  `type` is `u` for UID or `g` for GID.  `container` is the first ID as seen inside the container.  `disk` is the first corresponding ID on the underlying filesystem.  `range-size` is the number of consecutive IDs to map.  Unmapped IDs fall back to the container's `lxc.idmap`.  Example 1: `u:123:456:1` maps UID 123 in the container to UID 456 on the disk. Files owned by UID 456 on the disk will appear as UID 123 inside the container.  Example 2: `g:100:50:5` maps 5 consecutive GIDs, such that GIDs 100-104 in the container are mapped to GIDs 50-54 on the disk.  Example 3: `passthrough` identity-maps all UIDs and GIDs, meaning IDs inside the container will match the IDs on the disk.")

    keepattrs: Optional[PveBoolean] = Field(default=None, description="Inherit UID, GID and access mode from the mount point directory, if it exists already.")

    mountoptions: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Extra mount options for rootfs/mps.")

    mp: StrictStr = Field(description="Path to the mount point as seen from inside the container.  NOTE: Must not contain any symlinks for security reasons.")

    quota: Optional[PveBoolean] = Field(default=None, description="Enable user quotas inside the container (not supported with zfs subvolumes)")

    replicate: Optional[PveBoolean] = Field(default=None, description="Will include this volume to a storage replica job.")

    ro: Optional[PveBoolean] = Field(default=None, description="Read-only mount point")

    shared: Optional[PveBoolean] = Field(default=None, description="Mark this non-volume mount point as available on all nodes.  WARNING: This option does not share the mount point automatically, it assumes it is shared already!")

    size: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Volume size (read only value).")

    volume: StrictStr = Field(description="Volume, device or directory to mount into the container.")

    __properties: ClassVar[List[str]] = ["acl", "backup", "idmap", "keepattrs", "mountoptions", "mp", "quota", "replicate", "ro", "shared", "size", "volume"]




    @field_validator('idmap')
    def idmap_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"(?:^(?:passthrough|[ug]:[0-9]+:[0-9]+:[1-9][0-9]*(?:;[ug]:[0-9]+:[0-9]+:[1-9][0-9]*)*)$)", value):
            raise ValueError(r"must validate the regular expression /(?:^(?:passthrough|[ug]:[0-9]+:[0-9]+:[1-9][0-9]*(?:;[ug]:[0-9]+:[0-9]+:[1-9][0-9]*)*)$)/")
        return value



    @field_validator('mountoptions')
    def mountoptions_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"(?:(?:(discard|lazytime|noatime|nodev|noexec|nosuid))(;(?:(discard|lazytime|noatime|nodev|noexec|nosuid)))*)", value):
            raise ValueError(r"must validate the regular expression /(?:(?:(discard|lazytime|noatime|nodev|noexec|nosuid))(;(?:(discard|lazytime|noatime|nodev|noexec|nosuid)))*)/")
        return value







    @field_validator('size')
    def size_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^\d+(?:\.\d+)?[KMGT]?$", value):
            raise ValueError(r"must validate the regular expression /^\d+(?:\.\d+)?[KMGT]?$/")
        return value




    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PveMpConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            # `exclude_unset` keeps schema defaults out of the wire payload
            # when the user constructed the model directly (e.g.
            # `Req(vmid=100)` would otherwise pull in
            # `cores=1, cpulimit=0, …` from the spec defaults and PVE
            # rejects the request with 400 because it never set those).
            # `exclude_none` keeps None values out of the wire payload —
            # both for direct construction (None means "unset") and for
            # the from_dict path (where unspecified obj keys become
            # `obj.get("k") == None` but show up in `model_fields_set`).
            exclude_unset=True,
            exclude_none=True,
        )
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PveMpConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "acl": obj.get("acl"),
            "backup": obj.get("backup"),
            "idmap": obj.get("idmap"),
            "keepattrs": obj.get("keepattrs"),
            "mountoptions": obj.get("mountoptions"),
            "mp": obj.get("mp"),
            "quota": obj.get("quota"),
            "replicate": obj.get("replicate"),
            "ro": obj.get("ro"),
            "shared": obj.get("shared"),
            "size": obj.get("size"),
            "volume": obj.get("volume")
        })
        return _obj


    def to_shorthand(self) -> str:
        """Serialise this PveMpConfig into Proxmox's CLI-style shorthand
        string (``key=value,…``).

        The property marked ``x-pve-default-key`` is emitted positionally
        without a ``key=`` prefix; aliases collapse multiple property
        names to the same wire key.

        Example::

            >>> PveMpConfig(model='virtio', bridge='vmbr0', firewall=1).to_shorthand()
            'virtio,bridge=vmbr0,firewall=1'
        """
        parts: list[str] = []
        
        if self.volume is not None:
            parts.append(str(self.volume))
        
        
        if self.acl is not None:
            parts.append(f"acl={self.acl}")
        
        if self.backup is not None:
            parts.append(f"backup={self.backup}")
        
        if self.idmap is not None:
            parts.append(f"idmap={self.idmap}")
        
        if self.keepattrs is not None:
            parts.append(f"keepattrs={self.keepattrs}")
        
        if self.mountoptions is not None:
            parts.append(f"mountoptions={self.mountoptions}")
        
        if self.mp is not None:
            parts.append(f"mp={self.mp}")
        
        if self.quota is not None:
            parts.append(f"quota={self.quota}")
        
        if self.replicate is not None:
            parts.append(f"replicate={self.replicate}")
        
        if self.ro is not None:
            parts.append(f"ro={self.ro}")
        
        if self.shared is not None:
            parts.append(f"shared={self.shared}")
        
        if self.size is not None:
            parts.append(f"size={self.size}")
        
        return ",".join(parts)

    @classmethod
    def from_shorthand(cls, raw: Optional[str]) -> Optional["PveMpConfig"]:
        """Parse a Proxmox shorthand string into a PveMpConfig. Mirror of
        :meth:`to_shorthand`. The first comma-separated segment without
        ``=`` is interpreted as the default-key value.
        """
        if raw is None:
            return None
        kwargs: dict = {}
        default_key_consumed = False
        for part in raw.split(","):
            if not part:
                continue
            if "=" not in part:
                
                if not default_key_consumed:
                    kwargs["volume"] = part
                    default_key_consumed = True
                    continue
                
                continue
            k, _, v = part.partition("=")
            kwargs[k.replace("-", "_")] = v
        return cls(**kwargs)

