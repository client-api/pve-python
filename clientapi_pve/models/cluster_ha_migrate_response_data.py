# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from clientapi_pve.models.cluster_ha_migrate_response_data_blocking_resources_inner import ClusterHaMigrateResponseDataBlockingResourcesInner
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ClusterHaMigrateResponseData(BaseModel):
    """
    ClusterHaMigrateResponseData
    """ # noqa: E501

    blocking_resources: Optional[List[ClusterHaMigrateResponseDataBlockingResourcesInner]] = Field(default=None, description="HA resources, which are blocking the given HA resource from being migrated to the requested target node.", alias="blocking-resources")

    comigrated_resources: Optional[List[Any]] = Field(default=None, description="HA resources, which are migrated to the same requested target node as the given HA resource, because these are in positive affinity with the HA resource.", alias="comigrated-resources")

    requested_node: StrictStr = Field(description="Node, which was requested to be migrated to.", alias="requested-node")

    sid: StrictStr = Field(description="HA resource, which is requested to be migrated.")

    __properties: ClassVar[List[str]] = ["blocking-resources", "comigrated-resources", "requested-node", "sid"]







    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ClusterHaMigrateResponseData from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            # `exclude_unset` keeps schema defaults out of the wire payload
            # when the user constructed the model directly (e.g.
            # `Req(vmid=100)` would otherwise pull in
            # `cores=1, cpulimit=0, …` from the spec defaults and PVE
            # rejects the request with 400 because it never set those).
            # `exclude_none` keeps None values out of the wire payload —
            # both for direct construction (None means "unset") and for
            # the from_dict path (where unspecified obj keys become
            # `obj.get("k") == None` but show up in `model_fields_set`).
            exclude_unset=True,
            exclude_none=True,
        )
        
        # override the default output from pydantic by calling `to_dict()` of each item in blocking_resources (list)
        _items = []
        if self.blocking_resources:
            for _item_blocking_resources in self.blocking_resources:
                if _item_blocking_resources:
                    _items.append(_item_blocking_resources.to_dict())
            _dict['blocking-resources'] = _items
        
        
        
        
        
        
        
        
        
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ClusterHaMigrateResponseData from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "blocking-resources": [ClusterHaMigrateResponseDataBlockingResourcesInner.from_dict(_item) for _item in obj["blocking-resources"]] if obj.get("blocking-resources") is not None else None,
            "comigrated-resources": obj.get("comigrated-resources"),
            "requested-node": obj.get("requested-node"),
            "sid": obj.get("sid")
        })
        return _obj


