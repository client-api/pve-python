# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class PveErasureCodingConfig(BaseModel):
    """
    PveErasureCodingConfig
    """ # noqa: E501

    device_class: Optional[StrictStr] = Field(default=None, description="CRUSH device class. Will create an erasure coded pool plus a replicated pool for metadata.", alias="device-class")

    failure_domain: Optional[StrictStr] = Field(default='host', description="CRUSH failure domain. Default is 'host'. Will create an erasure coded pool plus a replicated pool for metadata.", alias="failure-domain")

    k: Annotated[int, Field(strict=True, ge=2)] = Field(description="Number of data chunks. Will create an erasure coded pool plus a replicated pool for metadata.")

    m: Annotated[int, Field(strict=True, ge=1)] = Field(description="Number of coding chunks. Will create an erasure coded pool plus a replicated pool for metadata.")

    profile: Optional[StrictStr] = Field(default=None, description="Override the erasure code (EC) profile to use. Will create an erasure coded pool plus a replicated pool for metadata.")

    __properties: ClassVar[List[str]] = ["device-class", "failure-domain", "k", "m", "profile"]








    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PveErasureCodingConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PveErasureCodingConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "device-class": obj.get("device-class"),
            "failure-domain": obj.get("failure-domain") if obj.get("failure-domain") is not None else 'host',
            "k": obj.get("k"),
            "m": obj.get("m"),
            "profile": obj.get("profile")
        })
        return _obj


    def to_shorthand(self) -> str:
        """Serialise this PveErasureCodingConfig into Proxmox's CLI-style shorthand
        string (``key=value,…``).

        The property marked ``x-pve-default-key`` is emitted positionally
        without a ``key=`` prefix; aliases collapse multiple property
        names to the same wire key.

        Example::

            >>> PveErasureCodingConfig(model='virtio', bridge='vmbr0', firewall=1).to_shorthand()
            'virtio,bridge=vmbr0,firewall=1'
        """
        parts: list[str] = []
        
        
        if self.device_class is not None:
            parts.append(f"device-class={self.device_class}")
        
        if self.failure_domain is not None:
            parts.append(f"failure-domain={self.failure_domain}")
        
        if self.k is not None:
            parts.append(f"k={self.k}")
        
        if self.m is not None:
            parts.append(f"m={self.m}")
        
        if self.profile is not None:
            parts.append(f"profile={self.profile}")
        
        return ",".join(parts)

    @classmethod
    def from_shorthand(cls, raw: Optional[str]) -> Optional["PveErasureCodingConfig"]:
        """Parse a Proxmox shorthand string into a PveErasureCodingConfig. Mirror of
        :meth:`to_shorthand`. The first comma-separated segment without
        ``=`` is interpreted as the default-key value.
        """
        if raw is None:
            return None
        kwargs: dict = {}
        default_key_consumed = False
        for part in raw.split(","):
            if not part:
                continue
            if "=" not in part:
                
                continue
            k, _, v = part.partition("=")
            kwargs[k.replace("-", "_")] = v
        return cls(**kwargs)

