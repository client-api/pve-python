# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ClusterMappingCreatePciRequestMapInnerOneOf(BaseModel):
    """
    ClusterMappingCreatePciRequestMapInnerOneOf
    """ # noqa: E501

    description: Optional[Annotated[str, Field(strict=True, max_length=4096)]] = Field(default=None, description="Description of the node specific device.")

    id: Annotated[str, Field(strict=True)] = Field(description="The vendor and device ID that is expected. Used for detecting hardware changes")

    iommugroup: Optional[StrictInt] = Field(default=None, description="The IOMMU group in which the device is to be expected in. Used for detecting hardware changes.")

    node: Annotated[str, Field(strict=True)] = Field(description="The cluster node name.")

    path: Annotated[str, Field(strict=True)] = Field(description="The path to the device. If the function is omitted, the whole device is mapped. In that case use the attributes of the first device. You can give multiple paths as a semicolon separated list, the first available will then be chosen on guest start.")

    subsystem_id: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The subsystem vendor and device ID that is expected. Used for detecting hardware changes.", alias="subsystem-id")

    __properties: ClassVar[List[str]] = ["description", "id", "iommugroup", "node", "path", "subsystem-id"]



    @field_validator('id')
    def id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"(?:^[0-9A-Fa-f]{4}:[0-9A-Fa-f]{4}$)", value):
            raise ValueError(r"must validate the regular expression /(?:^[0-9A-Fa-f]{4}:[0-9A-Fa-f]{4}$)/")
        return value



    @field_validator('node')
    def node_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(?:[a-zA-Z0-9](?:[a-zA-Z0-9\-]*[a-zA-Z0-9])?)$", value):
            raise ValueError(r"must validate the regular expression /^(?:[a-zA-Z0-9](?:[a-zA-Z0-9\-]*[a-zA-Z0-9])?)$/")
        return value


    @field_validator('path')
    def path_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"(?:[a-f0-9]{4,}:[a-f0-9]{2}:[a-f0-9]{2}(?:.[a-f0-9])?;)*[a-f0-9]{4,}:[a-f0-9]{2}:[a-f0-9]{2}(?:.[a-f0-9])?", value):
            raise ValueError(r"must validate the regular expression /(?:[a-f0-9]{4,}:[a-f0-9]{2}:[a-f0-9]{2}(?:.[a-f0-9])?;)*[a-f0-9]{4,}:[a-f0-9]{2}:[a-f0-9]{2}(?:.[a-f0-9])?/")
        return value


    @field_validator('subsystem_id')
    def subsystem_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"(?:^[0-9A-Fa-f]{4}:[0-9A-Fa-f]{4}$)", value):
            raise ValueError(r"must validate the regular expression /(?:^[0-9A-Fa-f]{4}:[0-9A-Fa-f]{4}$)/")
        return value



    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ClusterMappingCreatePciRequestMapInnerOneOf from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            # `exclude_unset` keeps schema defaults out of the wire payload
            # when the user constructed the model directly (e.g.
            # `Req(vmid=100)` would otherwise pull in
            # `cores=1, cpulimit=0, …` from the spec defaults and PVE
            # rejects the request with 400 because it never set those).
            # `exclude_none` keeps None values out of the wire payload —
            # both for direct construction (None means "unset") and for
            # the from_dict path (where unspecified obj keys become
            # `obj.get("k") == None` but show up in `model_fields_set`).
            exclude_unset=True,
            exclude_none=True,
        )
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ClusterMappingCreatePciRequestMapInnerOneOf from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "description": obj.get("description"),
            "id": obj.get("id"),
            "iommugroup": obj.get("iommugroup"),
            "node": obj.get("node"),
            "path": obj.get("path"),
            "subsystem-id": obj.get("subsystem-id")
        })
        return _obj


    def to_shorthand(self) -> str:
        """Serialise this ClusterMappingCreatePciRequestMapInnerOneOf into Proxmox's CLI-style shorthand
        string (``key=value,…``).

        The property marked ``x-pve-default-key`` is emitted positionally
        without a ``key=`` prefix; aliases collapse multiple property
        names to the same wire key.

        Example::

            >>> ClusterMappingCreatePciRequestMapInnerOneOf(model='virtio', bridge='vmbr0', firewall=1).to_shorthand()
            'virtio,bridge=vmbr0,firewall=1'
        """
        parts: list[str] = []
        
        
        if self.description is not None:
            parts.append(f"description={self.description}")
        
        if self.id is not None:
            parts.append(f"id={self.id}")
        
        if self.iommugroup is not None:
            parts.append(f"iommugroup={self.iommugroup}")
        
        if self.node is not None:
            parts.append(f"node={self.node}")
        
        if self.path is not None:
            parts.append(f"path={self.path}")
        
        if self.subsystem_id is not None:
            parts.append(f"subsystem-id={self.subsystem_id}")
        
        return ",".join(parts)

    @classmethod
    def from_shorthand(cls, raw: Optional[str]) -> Optional["ClusterMappingCreatePciRequestMapInnerOneOf"]:
        """Parse a Proxmox shorthand string into a ClusterMappingCreatePciRequestMapInnerOneOf. Mirror of
        :meth:`to_shorthand`. The first comma-separated segment without
        ``=`` is interpreted as the default-key value.
        """
        if raw is None:
            return None
        kwargs: dict = {}
        default_key_consumed = False
        for part in raw.split(","):
            if not part:
                continue
            if "=" not in part:
                
                continue
            k, _, v = part.partition("=")
            kwargs[k.replace("-", "_")] = v
        return cls(**kwargs)

