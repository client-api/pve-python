# coding: utf-8

"""
    Proxmox Virtual Environment API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from clientapi_pve.models.pve_boolean import PveBoolean
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class NodesCephGetMdsResponseDataInner(BaseModel):
    """
    NodesCephGetMdsResponseDataInner
    """ # noqa: E501

    addr: Optional[StrictStr] = Field(default=None, description="Address as advertised by the MDS; Ceph-formatted (typically 'IP:PORT/NONCE').")

    ceph_version: Optional[StrictStr] = Field(default=None, description="Full Ceph version string of the MDS daemon.")

    ceph_version_short: Optional[StrictStr] = Field(default=None, description="Short Ceph version string of the MDS daemon (e.g. '19.2.0').")

    direxists: Optional[PveBoolean] = Field(default=None, description="Set when the MDS's data directory exists on this node.")

    fs_name: Optional[StrictStr] = Field(default=None, description="Name of the CephFS this MDS is bound to; absent or null for standby MDSes not currently serving a rank.")

    host: Optional[StrictStr] = Field(default=None, description="Host the MDS runs on.")

    name: StrictStr = Field(description="The name (ID) for the MDS.")

    rank: Optional[int] = Field(default=None, description="MDS rank within the file system; -1 for standby MDSes not currently bound to a rank.")

    service: Optional[PveBoolean] = Field(default=None, description="Set if a ceph-mds@<id> systemd unit is enabled on the hosting node; absent otherwise.")

    standby_replay: Optional[PveBoolean] = Field(default=None, description="If true, the standby MDS is polling the active MDS for faster recovery (hot standby).")

    state: StrictStr = Field(description="MDS state: Ceph-reported run state (e.g. 'up:active', 'up:standby', 'up:standby-replay') for daemons known to the cluster; 'stopped' or 'unknown' for configured daemons not visible to the cluster.")

    __properties: ClassVar[List[str]] = ["addr", "ceph_version", "ceph_version_short", "direxists", "fs_name", "host", "name", "rank", "service", "standby_replay", "state"]














    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of NodesCephGetMdsResponseDataInner from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            # `exclude_unset` keeps schema defaults out of the wire payload
            # when the user constructed the model directly (e.g.
            # `Req(vmid=100)` would otherwise pull in
            # `cores=1, cpulimit=0, …` from the spec defaults and PVE
            # rejects the request with 400 because it never set those).
            # `exclude_none` keeps None values out of the wire payload —
            # both for direct construction (None means "unset") and for
            # the from_dict path (where unspecified obj keys become
            # `obj.get("k") == None` but show up in `model_fields_set`).
            exclude_unset=True,
            exclude_none=True,
        )
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of NodesCephGetMdsResponseDataInner from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "addr": obj.get("addr"),
            "ceph_version": obj.get("ceph_version"),
            "ceph_version_short": obj.get("ceph_version_short"),
            "direxists": obj.get("direxists"),
            "fs_name": obj.get("fs_name"),
            "host": obj.get("host"),
            "name": obj.get("name"),
            "rank": obj.get("rank"),
            "service": obj.get("service"),
            "standby_replay": obj.get("standby_replay"),
            "state": obj.get("state")
        })
        return _obj


